export interface ContentfulTag {
  id: string;
  name: string;
}

const tags: ContentfulTag[] = [
  { id: "backToBackSwe", name: "Back to back SWE" },
  { id: "primitives", name: "Primitives" },
];

export default tags;
